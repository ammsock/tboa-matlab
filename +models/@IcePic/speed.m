% function sp = speed(obj,p)

%   vf = obj.vectorfield(p)

%   len = vecnorm(vf(2:end,:),2);
%   % for k=1:size(x,1)
%   %   vf = obj.vectorfield(x(k,:));
%   %   len(k) = norm(vf(2:end),2);
%   % end
% end
